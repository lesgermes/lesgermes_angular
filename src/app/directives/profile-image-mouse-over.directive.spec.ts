import { ProfileImageMouseOverDirective } from './profile-image-mouse-over.directive';

describe('ProfileImageMouseOverDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileImageMouseOverDirective();
    expect(directive).toBeTruthy();
  });
});
